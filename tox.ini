[tox]
envlist = py310
skipsdist = True


[testenv]
deps =
    pytest
    coverage
    aiohttp
    pytest-asyncio
    pydantic
commands =
    coverage run -m pytest -v --asyncio-mode=auto
    coverage xml

[coverage:run]
relative_files = True
source = aduro/
branch = True
